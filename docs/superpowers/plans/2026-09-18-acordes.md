# Timbre medido em acordes — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `build` mede o timbre em partes de acorde (base sem nota isolada) e aceita uma janela de tempo para o alvo (issue #1).

**Architecture:** o tone-analyzer ganha `levels_at` (nível em frequências dadas) e `detect_chords` (ataque → conjunto de notas, detectores `salience` e `basic-pitch`). O tone-builder ganha o alvo de acorde (frequências k·f0 de cada nota, sem as de dono ambíguo), o DI do acorde (soma de notas da biblioteca por voicing tocável, ou acorde gravado), a escolha do DI por medição (como a corda) e o `validate --chords`.

**Tech Stack:** Python 3.11+, numpy, scipy, soundfile, pytest; `basic-pitch` como extra opcional do tone-analyzer.

**Spec:** `docs/superpowers/specs/2026-09-18-acordes-design.md`

## Global Constraints

- Onde trabalhar: tone-builder em `tone-builder/.solvers/issue-1/` (já clonado); tone-analyzer em `tone-analyzer/.solvers/issue-chords/` (`git clone git@github.com:jpfaria/tone-analyzer.git .solvers/issue-chords`, com `GIT_LFS_SKIP_SMUDGE=1`). Nunca `git worktree`, nunca editar a pasta principal.
- Todo commit é seguido de `git fetch && git pull --rebase && git push` na `main`.
- Fronteira: informação de UM áudio (detectar notas, ler níveis) → tone-analyzer; comparar/decidir → tone-builder.
- Portões do método sem mudança: destaque ≥ 13 dB, ≥ 5 frequências aceitas, janela de 0,6 s, 48 kHz.
- Colisão entre notas do acorde: frequências a menos de 2,4 % uma da outra saem.
- Voicing: uma nota por corda, cordas distintas, casas pressionadas com `max − min ≤ 3` (corda solta não conta), no máximo 32 voicings por acorde.
- Critério do `validate --chords` a −6 dB: conjunto certo ≥ 90 %, zero nota falsa, erro de nível ≤ 2 dB.
- Libs próprias sem pin (`tone-analyzer @ git+https://github.com/jpfaria/tone-analyzer`).
- Editar `skills/tone-builder/SKILL.md` exige invocar `superpowers:writing-skills` antes.
- Nenhum teste toca estado fora do repo e do `tmp_path` (sem gravar em `~/.tone-analyzer`, `~/.tone-builder`, `~/.claude`).

---

## Parte A — tone-analyzer

### Task 1: `levels_at` e `harmonic_levels` em cima dela

**Files:**
- Create: `tone_analyzer/chords.py`
- Modify: `tone_analyzer/notes.py` (`harmonic_levels`, linhas 130–171)
- Modify: `.gitignore` (acrescentar `.solvers/`)
- Test: `tests/test_chords.py`

**Interfaces:**
- Produces: `levels_at(signal, sr, start_s, freqs_hz: list[float], dur_s=0.6) -> dict | None` com `level_db`, `neighbour_db`, `prominence_db` (listas alinhadas a `freqs_hz`, `None` acima de 0,9·Nyquist); `None` se sobra < 0,3 s.

- [ ] **Step 1: Clonar e preparar**

```bash
cd ~/Projetos/github.com/jpfaria/tone-analyzer
GIT_LFS_SKIP_SMUDGE=1 git clone git@github.com:jpfaria/tone-analyzer.git .solvers/issue-chords
cd .solvers/issue-chords && [ -d .git ] && python3 -m venv .venv && .venv/bin/pip install -q -e ".[dev]"
grep -qx '.solvers/' .gitignore || echo '.solvers/' >> .gitignore
```

- [ ] **Step 2: Teste que falha**

```python
"""Tests for tone_analyzer.chords — which notes sound at one attack, and levels at given frequencies."""

from __future__ import annotations

import numpy as np
import pytest

from tests._synth import harmonic_note, midi_to_hz
from tone_analyzer import chords, notes

SR = 48000
HARM = [0.0, -3.0, -6.0, -9.0, -12.0, -15.0, -18.0, -21.0, -24.0, -27.0, -30.0, -33.0]


def test_levels_at_matches_harmonic_levels_on_a_single_note():
    x = np.concatenate([np.zeros(SR // 2, np.float32), harmonic_note(57, SR, 1.5, HARM)])
    f0 = midi_to_hz(57)
    h = notes.harmonic_levels(x, SR, 0.5, f0)
    lv = chords.levels_at(x, SR, 0.5, [f0 * k for k in range(1, 17)])
    assert lv["level_db"] == pytest.approx(h["level_db"])
    assert lv["prominence_db"] == pytest.approx(h["prominence_db"])


def test_levels_at_none_when_too_short():
    assert chords.levels_at(np.zeros(SR // 10), SR, 0.0, [440.0]) is None
```

- [ ] **Step 3: Rodar e ver falhar**

Run: `.venv/bin/pytest tests/test_chords.py -v`
Expected: FAIL (`ImportError: cannot import name 'chords'`)

- [ ] **Step 4: Implementar**

`tone_analyzer/chords.py`:

```python
"""Which notes sound at one attack of ONE guitar audio, and levels at given frequencies.

Pure functions, no I/O. The level reading is the one validated for single notes
(Hann window over 0.6 s from the attack, peak within +-1.2 %, neighbourhood median
at 0.90-0.96 and 1.04-1.10 of the frequency), taken at any list of frequencies so a
chord can be read at the harmonics of each of its notes.
"""

from __future__ import annotations

import numpy as np

PEAK_TOL = 0.012
NEIGH_LO = (0.90, 0.96)
NEIGH_HI = (1.04, 1.10)


def levels_at(signal: np.ndarray, sr: int, start_s: float, freqs_hz: list[float],
              dur_s: float = 0.6) -> dict | None:
    x = np.asarray(signal, dtype=np.float64)
    ini = int(round(start_s * sr))
    n = min(int(round(dur_s * sr)), len(x) - ini)
    if n < int(0.3 * sr):
        return None
    seg = x[ini:ini + n] * np.hanning(n)
    power = np.abs(np.fft.rfft(seg, 4 * n)) ** 2
    freqs = np.fft.rfftfreq(4 * n, 1 / sr)
    level: list[float | None] = []
    neigh: list[float | None] = []
    prom: list[float | None] = []
    for f in freqs_hz:
        peak = (freqs > f * (1 - PEAK_TOL)) & (freqs < f * (1 + PEAK_TOL))
        around = (((freqs > f * NEIGH_LO[0]) & (freqs < f * NEIGH_LO[1]))
                  | ((freqs > f * NEIGH_HI[0]) & (freqs < f * NEIGH_HI[1])))
        if f > 0.9 * sr / 2 or not peak.any() or not around.any():
            level.append(None)
            neigh.append(None)
            prom.append(None)
            continue
        lv = 10.0 * np.log10(power[peak].max() + 1e-30)
        nb = 10.0 * np.log10(np.median(power[around]) + 1e-30)
        level.append(float(lv))
        neigh.append(float(nb))
        prom.append(float(lv - nb))
    return {"level_db": level, "neighbour_db": neigh, "prominence_db": prom}
```

`tone_analyzer/notes.py`: substituir o corpo de `harmonic_levels` (manter assinatura e docstring) por:

```python
    from tone_analyzer.chords import levels_at
    h = levels_at(signal, sr, start_s, [f0_hz * k for k in range(1, n_harm + 1)], dur_s=dur_s)
    if h is None:
        return None
    h1 = h["level_db"][0]
    h["relative_db"] = [None if (v is None or h1 is None) else float(v - h1) for v in h["level_db"]]
    return h
```

e apagar `PEAK_TOL`, `NEIGH_LO`, `NEIGH_HI` de `notes.py` somente se nenhum outro módulo os importar (`grep -rn "notes.PEAK_TOL\|notes.NEIGH" tone_analyzer tests`); se importarem, reexportar: `from tone_analyzer.chords import PEAK_TOL, NEIGH_LO, NEIGH_HI`.

- [ ] **Step 5: Rodar a suíte inteira**

Run: `.venv/bin/pytest -q`
Expected: tudo PASS (nenhum teste antigo de `harmonic_levels` muda de número).

- [ ] **Step 6: Commit + push**

```bash
git add .gitignore tone_analyzer/chords.py tone_analyzer/notes.py tests/test_chords.py
git commit -m "feat(chords): levels_at reads level and prominence at any frequencies; harmonic_levels uses it"
git fetch && git pull --rebase && git push
```

---

### Task 2: `detect_chords` por saliência harmônica

**Files:**
- Modify: `tone_analyzer/chords.py`
- Test: `tests/test_chords.py`

**Interfaces:**
- Consumes: `levels_at` (Task 1), `notes.note_onsets`, `notes.midi_name`.
- Produces: `detect_chords(signal, sr, dur_s=0.6, detector="salience") -> list[dict]`, cada `{"start_s": float, "midis": list[int] (crescente, ≥ 2), "names": list[str]}`; `DETECTORS = ("salience", "basic-pitch")`; `salience_set(signal, sr, start_s, dur_s=0.6) -> list[int]`.

- [ ] **Step 1: Testes que falham** (acrescentar em `tests/test_chords.py`)

```python
def _chord(midis, stagger_s=0.0, dur_s=1.5, lead_s=0.5):
    parts = []
    for i, m in enumerate(midis):
        pre = np.zeros(int(round((lead_s + i * stagger_s) * SR)), np.float32)
        parts.append(np.concatenate([pre, harmonic_note(m, SR, dur_s, HARM)]))
    n = max(len(p) for p in parts)
    x = sum(np.pad(p, (0, n - len(p))) for p in parts)
    return (0.5 * x / np.abs(x).max()).astype(np.float32)


@pytest.mark.parametrize("midis", [[40, 47], [40, 47, 52], [45, 52, 57, 61, 64], [43, 47, 50, 55, 59, 67]])
def test_salience_finds_the_chord(midis):
    x = _chord(midis, stagger_s=0.01)
    assert chords.salience_set(x, SR, 0.5) == midis


def test_single_note_is_not_a_chord():
    x = _chord([52])
    assert chords.salience_set(x, SR, 0.5) == [52]
    assert chords.detect_chords(x, SR) == []


def test_detect_chords_reports_attack_and_names():
    x = np.concatenate([_chord([40, 47, 52]), _chord([45, 52, 57])])
    found = chords.detect_chords(x, SR)
    assert [c["midis"] for c in found] == [[40, 47, 52], [45, 52, 57]]
    assert found[0]["names"] == ["E2", "B2", "E3"]
    assert found[0]["start_s"] == pytest.approx(0.5, abs=0.03)


def test_unknown_detector_is_an_error():
    with pytest.raises(ValueError):
        chords.detect_chords(_chord([40, 47]), SR, detector="nope")
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `.venv/bin/pytest tests/test_chords.py -v`
Expected: FAIL (`AttributeError: ... 'salience_set'`)

- [ ] **Step 3: Implementar** (acrescentar em `tone_analyzer/chords.py`)

```python
from tone_analyzer.notes import midi_name, note_onsets

DETECTORS = ("salience", "basic-pitch")
SAL_PROM_DB = 13.0        # the method's prominence gate
SAL_HARM = 8              # harmonics that vote for a candidate
EXPLAIN_HARM = 16         # harmonics a chosen note takes away from the others
MIN_FREE = 3              # a candidate needs this many prominent, unexplained harmonics
MAX_NOTES = 6             # six strings
COLLIDE = 0.024           # two frequencies closer than this are the same peak
MIDI_RANGE = range(40, 89)   # E2..E6


def _hz(midi: int) -> float:
    return 440.0 * 2.0 ** ((midi - 69) / 12.0)


def salience_set(signal: np.ndarray, sr: int, start_s: float, dur_s: float = 0.6) -> list[int]:
    """Greedy: the candidate with the most prominence on unexplained harmonics wins, its
    harmonics become explained, repeat. An octave or fifth above a chosen note only has
    explained harmonics, so it never gets MIN_FREE."""
    peaks: dict[int, list[tuple[float, float]]] = {}
    for m in MIDI_RANGE:
        f0 = _hz(m)
        h = levels_at(signal, sr, start_s, [f0 * k for k in range(1, SAL_HARM + 1)], dur_s)
        if h is None:
            return []
        peaks[m] = [(f0 * k, p) for k, p in enumerate(h["prominence_db"], 1)
                    if p is not None and p >= SAL_PROM_DB]
    explained: list[float] = []
    chosen: list[int] = []
    while len(chosen) < MAX_NOTES:
        best, best_s = None, 0.0
        for m, pk in peaks.items():
            if m in chosen:
                continue
            free = [p for f, p in pk if not any(abs(f - e) / e < COLLIDE for e in explained)]
            if len(free) >= MIN_FREE and sum(free) > best_s:
                best, best_s = m, sum(free)
        if best is None:
            break
        chosen.append(best)
        explained += [_hz(best) * k for k in range(1, EXPLAIN_HARM + 1)]
    return sorted(chosen)


def detect_chords(signal: np.ndarray, sr: int, dur_s: float = 0.6, detector: str = "salience") -> list[dict]:
    """One entry per attack where 2+ notes sound over dur_s. Single notes stay with notes.detect_notes."""
    if detector not in DETECTORS:
        raise ValueError(f"unknown chord detector {detector!r} ({', '.join(DETECTORS)})")
    x = np.asarray(signal, dtype=np.float64)
    span = int(round(dur_s * sr))
    pick = salience_set if detector == "salience" else _basic_pitch_picker(x, sr)
    out = []
    for a in note_onsets(x, sr):
        if a + span >= len(x):
            continue
        midis = pick(x, sr, a / sr, dur_s)
        if len(midis) >= 2:
            out.append({"start_s": a / sr, "midis": midis, "names": [midi_name(m) for m in midis]})
    return out


def _basic_pitch_picker(x: np.ndarray, sr: int):
    raise ValueError("basic-pitch detector: not implemented yet")   # replaced in Task 3
```

- [ ] **Step 4: Rodar**

Run: `.venv/bin/pytest tests/test_chords.py -v`
Expected: PASS. Se um caso paramétrico falhar, depurar com `superpowers:systematic-debugging` (imprimir `peaks` e `free` por candidato) antes de mexer em constante; qualquer constante alterada vai com o motivo medido no docstring.

- [ ] **Step 5: Commit + push**

```bash
git add tone_analyzer/chords.py tests/test_chords.py
git commit -m "feat(chords): detect_chords with harmonic salience (greedy, explained harmonics removed)"
git fetch && git pull --rebase && git push
```

---

### Task 3: detector `basic-pitch` (extra opcional)

**Files:**
- Modify: `tone_analyzer/chords.py` (`_basic_pitch_picker`)
- Modify: `pyproject.toml` (`[project.optional-dependencies] basic-pitch = [...]`)
- Test: `tests/test_chords.py`

**Interfaces:**
- Produces: `detect_chords(..., detector="basic-pitch")`; sem o extra instalado → `ValueError` com `pip install 'tone-analyzer[basic-pitch]'`.

- [ ] **Step 1: Conferir que instala neste Python**

```bash
.venv/bin/python --version
.venv/bin/pip install -q "basic-pitch[onnx]" && .venv/bin/python -c "from basic_pitch.inference import predict; from basic_pitch import ICASSP_2022_MODEL_PATH; print(ICASSP_2022_MODEL_PATH)"
```

Se não instalar (sem wheel para esta versão do Python/arquitetura): parar a Task 3, anotar o erro exato em `docs/learnings.md` do tone-analyzer, e avisar o João em uma linha — o detector sai do escopo e o padrão fica `salience`. Não trocar de modelo por conta própria.

- [ ] **Step 2: Testes que falham**

```python
bp = pytest.importorskip("basic_pitch")


def test_basic_pitch_finds_a_power_chord():
    x = np.concatenate([_chord([40, 47, 52]), np.zeros(SR, np.float32)])
    found = chords.detect_chords(x, SR, detector="basic-pitch")
    assert found and found[0]["midis"] == [40, 47, 52]


def test_basic_pitch_missing_gives_install_hint(monkeypatch):
    import builtins
    real = builtins.__import__

    def fake(name, *a, **k):
        if name.startswith("basic_pitch"):
            raise ImportError(name)
        return real(name, *a, **k)
    monkeypatch.setattr(builtins, "__import__", fake)
    with pytest.raises(ValueError, match=r"tone-analyzer\[basic-pitch\]"):
        chords.detect_chords(_chord([40, 47]), SR, detector="basic-pitch")
```

(O segundo teste fica fora do `importorskip`: colocar o `importorskip` dentro do primeiro teste.)

- [ ] **Step 3: Rodar e ver falhar**

Run: `.venv/bin/pytest tests/test_chords.py -k basic -v`
Expected: FAIL (`not implemented yet`)

- [ ] **Step 4: Implementar**

```python
BP_ACTIVE = 0.75   # a note counts when it sounds over this fraction of the window


def _basic_pitch_picker(x: np.ndarray, sr: int):
    try:
        from basic_pitch import ICASSP_2022_MODEL_PATH
        from basic_pitch.inference import predict
    except ImportError as e:
        raise ValueError("basic-pitch detector needs: pip install 'tone-analyzer[basic-pitch]'") from e
    import tempfile
    from pathlib import Path

    import soundfile as sf
    with tempfile.TemporaryDirectory(prefix="tone-analyzer-bp-") as tmp:
        wav = Path(tmp) / "in.wav"
        sf.write(str(wav), x.astype(np.float32), sr, subtype="FLOAT")
        _, _, events = predict(str(wav), ICASSP_2022_MODEL_PATH)
    events = [(float(e[0]), float(e[1]), int(e[2])) for e in events]

    def pick(_x, _sr, start_s: float, dur_s: float) -> list[int]:
        end = start_s + dur_s
        on = {m for s, e, m in events if min(e, end) - max(s, start_s) >= BP_ACTIVE * dur_s}
        return sorted(m for m in on if m in MIDI_RANGE)
    return pick
```

`pyproject.toml`:

```toml
[project.optional-dependencies]
dev = ["pytest==8.3.3"]
basic-pitch = ["basic-pitch[onnx]"]
```

(usar a versão exata que o Step 1 instalou, `basic-pitch[onnx]==X.Y.Z`.)

- [ ] **Step 5: Rodar**

Run: `.venv/bin/pytest tests/test_chords.py -v`
Expected: PASS

- [ ] **Step 6: Commit + push**

```bash
git add tone_analyzer/chords.py pyproject.toml tests/test_chords.py
git commit -m "feat(chords): basic-pitch detector as optional extra"
git fetch && git pull --rebase && git push
```

---

### Task 4: CLI `tone-analyzer chords`, versão 0.5.0

**Files:**
- Modify: `tone_analyzer/chords.py` (`main`)
- Modify: `tone_analyzer/cli.py` (`_COMMANDS`, `USAGE`)
- Modify: `pyproject.toml` e `.claude-plugin/plugin.json` (versão `0.5.0`), `README.md` (uma linha do comando)
- Test: `tests/test_chords.py`

**Interfaces:**
- Produces: `tone-analyzer chords AUDIO [--detector salience|basic-pitch] [--out FILE]` → JSON `{"detector", "chords": [...]}` no stdout ou em `--out`.

- [ ] **Step 1: Teste que falha**

```python
def test_cli_chords_writes_json(tmp_path, capsys):
    import json

    import soundfile as sf

    from tone_analyzer import cli
    wav = tmp_path / "c.wav"
    sf.write(str(wav), _chord([40, 47, 52]), SR, subtype="FLOAT")
    assert cli.main(["chords", str(wav)]) == 0
    out = json.loads(capsys.readouterr().out)
    assert out["detector"] == "salience"
    assert out["chords"][0]["midis"] == [40, 47, 52]
```

- [ ] **Step 2: Rodar e ver falhar** — `.venv/bin/pytest tests/test_chords.py::test_cli_chords_writes_json -v` → FAIL (`unknown command 'chords'`)

- [ ] **Step 3: Implementar**

Em `chords.py`:

```python
def main(argv: list[str] | None = None) -> int:
    import argparse
    import json
    from pathlib import Path

    import soundfile as sf
    p = argparse.ArgumentParser(prog="tone-analyzer chords")
    p.add_argument("audio")
    p.add_argument("--detector", default="salience", choices=DETECTORS)
    p.add_argument("--out")
    a = p.parse_args(argv)
    x, sr = sf.read(a.audio, always_2d=False)
    if x.ndim > 1:
        x = x.mean(axis=1)
    try:
        found = detect_chords(x, sr, detector=a.detector)
    except ValueError as e:
        print(f"chords: {e}", file=__import__("sys").stderr)
        return 2
    text = json.dumps({"detector": a.detector, "chords": found}, indent=1)
    if a.out:
        Path(a.out).write_text(text)
    else:
        print(text)
    return 0
```

Em `cli.py`: importar `chords`, `"chords": chords.main` em `_COMMANDS`, e em `USAGE` a linha
`  chords         <in.wav> [--detector salience|basic-pitch] [--out F]  notes sounding at each attack with 2+ notes`.

- [ ] **Step 4: Rodar tudo** — `.venv/bin/pytest -q` → PASS

- [ ] **Step 5: Commit + push**

```bash
git add tone_analyzer/chords.py tone_analyzer/cli.py pyproject.toml .claude-plugin/plugin.json README.md tests/test_chords.py
git commit -m "feat(cli): tone-analyzer chords; 0.5.0"
git fetch && git pull --rebase && git push
```

---

## Parte B — tone-builder (em `tone-builder/.solvers/issue-1/`)

Antes da Task 5: `./bootstrap.sh && GIT_LFS_SKIP_SMUDGE=1 .venv/bin/pip install -q --force-reinstall --no-deps "tone-analyzer @ git+https://github.com/jpfaria/tone-analyzer"` e conferir `.venv/bin/python -c "from tone_analyzer.chords import detect_chords"`. Rodar `.venv/bin/pytest -q` para ter a linha de base verde.

### Task 5: janela de tempo do alvo

**Files:**
- Modify: `tone_builder/target.py` (`build_target`)
- Modify: `tone_builder/build.py` (`build_tone`)
- Modify: `tone_builder/cli.py` (`_target`, `_build`, parsers `target` e `build`)
- Test: `tests/test_target.py`, `tests/test_cli.py`

**Interfaces:**
- Produces: `Window = tuple[float | None, float | None]` em `target.py`; `in_window(start_s, window) -> bool`; `build_target(disc, lead, available_midis, window=None, stats=None)`; `build_tone(..., window=None)`; `cli.parse_time("2:50") -> 170.0`; flags `--from` / `--to` (dest `t_from` / `t_to`).

- [ ] **Step 1: Testes que falham**

`tests/test_target.py`:

```python
from tone_builder.target import in_window


def test_window_keeps_only_attacks_inside_it():
    x = np.concatenate([note(62), note(64)])   # attacks at 0.5 s and 3.0 s
    assert [n["midi"] for n in build_target(x, x, {62, 64}, window=(2.0, None))] == [64]
    assert [n["midi"] for n in build_target(x, x, {62, 64}, window=(None, 2.0))] == [62]


def test_in_window_bounds():
    assert in_window(1.0, None) and in_window(1.0, (1.0, 2.0)) and not in_window(2.0, (1.0, 2.0))
```

`tests/test_cli.py`:

```python
def test_parse_time():
    assert cli.parse_time("2:50") == 170.0
    assert cli.parse_time("95.5") == 95.5
```

- [ ] **Step 2: Rodar e ver falhar** — `.venv/bin/pytest tests/test_target.py tests/test_cli.py -v` → FAIL (`ImportError: in_window`)

- [ ] **Step 3: Implementar**

`target.py`:

```python
Window = tuple[float | None, float | None]


def in_window(start_s: float, window: Window | None) -> bool:
    """[from, to): an attack exactly at `to` belongs to the next part."""
    if window is None:
        return True
    lo, hi = window
    return (lo is None or start_s >= lo) and (hi is None or start_s < hi)
```

Em `build_target`, parâmetros `window: Window | None = None, stats: dict | None = None`, e logo depois do `for n in detect_notes(...)`: `if not in_window(n["start_s"], window): continue`. (`stats` é usado na Task 7; aqui só aceitar.)

`build.py`: `build_tone(..., name, window=None)` e `build_target(disc, lead, set(by_midi), window=window)`.

`cli.py`:

```python
def parse_time(s: str) -> float:
    """M:SS or seconds."""
    if ":" in s:
        m, sec = s.split(":", 1)
        return int(m) * 60 + float(sec)
    return float(s)


def _window(a):
    lo = parse_time(a.t_from) if a.t_from else None
    hi = parse_time(a.t_to) if a.t_to else None
    return None if lo is None and hi is None else (lo, hi)
```

Nos parsers `target` e `build`: `add_argument("--from", dest="t_from", help="target attacks from M:SS")` e `add_argument("--to", dest="t_to", help="target attacks before M:SS")`; passar `window=_window(a)` em `_target` e em `_build`.

- [ ] **Step 4: Rodar** — `.venv/bin/pytest -q` → PASS

- [ ] **Step 5: Commit + push**

```bash
git add tone_builder/target.py tone_builder/build.py tone_builder/cli.py tests/test_target.py tests/test_cli.py
git commit -m "target: --from/--to window on build and target (issue #1)"
git fetch && git pull --rebase && git push
```

---

### Task 6: alvo e comparação por lista de frequências

Prepara o terreno: nota e acorde passam a ser lidos pela mesma rotina.

**Files:**
- Modify: `tone_builder/target.py`, `tone_builder/compare.py`, `tone_builder/render.py` (tag do arquivo)
- Test: `tests/test_target.py`, `tests/test_compare.py`

**Interfaces:**
- Produces: `N_HARM = 16`; `note_freqs(midi) -> list[float]`; `freqs_of(entry) -> list[float]` (usa `entry["freqs_hz"]`, ou `note_freqs(entry["midi"])` em `target.json` antigo); `read_levels(disc, start_s, freqs) -> tuple[list, list[bool]] | None`; toda entrada de nota ganha `"kind": "note"` e `"freqs_hz"`; `entry_tag(entry) -> str` (`"62"` ou `"40_47_52"`).

- [ ] **Step 1: Testes que falham**

`tests/test_target.py`:

```python
from tone_builder.target import freqs_of, midi_hz


def test_note_entry_carries_kind_and_frequencies():
    x = note(62)
    t = build_target(x, x, {62})[0]
    assert t["kind"] == "note"
    assert t["freqs_hz"][2] == pytest.approx(3 * midi_hz(62))
    assert len(t["freqs_hz"]) == len(t["level_db"]) == len(t["accepted"])


def test_old_target_json_without_frequencies_still_reads():
    assert freqs_of({"midi": 69})[1] == pytest.approx(880.0)
```

(`import pytest` no topo.)

`tests/test_compare.py`:

```python
def test_deviation_points_are_at_the_entry_frequencies():
    t = _target()
    d = note_deviation(t, note(62, start_s=0.02))
    assert {round(hz, 3) for hz, _ in d["points"]} <= {round(f, 3) for f in t["freqs_hz"]}
```

- [ ] **Step 2: Rodar e ver falhar** — `.venv/bin/pytest tests/test_target.py tests/test_compare.py -v` → FAIL (`KeyError: 'kind'`)

- [ ] **Step 3: Implementar**

`target.py`:

```python
from tone_analyzer.chords import levels_at

N_HARM = 16


def note_freqs(midi: int) -> list[float]:
    f0 = midi_hz(midi)
    return [f0 * k for k in range(1, N_HARM + 1)]


def freqs_of(entry: dict) -> list[float]:
    return entry.get("freqs_hz") or note_freqs(entry["midi"])


def read_levels(disc: np.ndarray, start_s: float, freqs: list[float]) -> tuple[list, list[bool]] | None:
    h = levels_at(disc, SR, start_s, freqs, dur_s=DUR_S)
    if h is None:
        return None
    return h["level_db"], [p is not None and p >= PROMINENCE_DB for p in h["prominence_db"]]
```

e em `build_target`, trocar o bloco `harmonic_levels … accepted` por:

```python
        freqs = note_freqs(n["midi"])
        r = read_levels(disc, n["start_s"], freqs)
        if r is None:
            continue
        level, accepted = r
        if sum(accepted) < MIN_HARMONICS:
            continue
        out.append({"kind": "note", "start_s": n["start_s"], "midi": n["midi"], "name": midi_name(n["midi"]),
                    "freqs_hz": freqs, "level_db": level, "accepted": accepted})
```

(remover o import de `harmonic_levels` se não sobrar uso.)

`compare.py`, `note_deviation`:

```python
def note_deviation(target_note: dict, wet: np.ndarray) -> dict | None:
    """wet: the same note or chord rendered by a candidate, mono at 48 kHz."""
    onset = take_onset(wet, SR)
    if onset is None:
        return None
    freqs = freqs_of(target_note)
    h = levels_at(wet, SR, onset / SR, freqs, dur_s=DUR_S)
    if h is None:
        return None
    ks = [k for k, (ok, t, w) in enumerate(zip(target_note["accepted"], target_note["level_db"], h["level_db"]))
          if ok and t is not None and w is not None]
    if len(ks) < MIN_HARMONICS:
        return None
    d = np.array([target_note["level_db"][k] - h["level_db"][k] for k in ks])
    d = d - d.mean()
    return {"rms_db": float(np.sqrt(np.mean(d ** 2))), "harmonics": len(ks),
            "points": [(freqs[k], float(v)) for k, v in zip(ks, d)]}
```

(imports: `from tone_analyzer.chords import levels_at`; `from tone_builder.target import DUR_S, MIN_HARMONICS, freqs_of`.)

`render.py`: `from tone_builder.target import entry_tag` e em `measure` o tag passa a `f"{i:02d}-{entry_tag(a['note'])}"`; em `target.py`:

```python
def entry_tag(entry: dict) -> str:
    return "_".join(str(m) for m in (entry.get("midis") or [entry["midi"]]))
```

- [ ] **Step 4: Rodar** — `.venv/bin/pytest -q` → PASS (inclui `test_build`, `test_verify`, `test_validator`)

- [ ] **Step 5: Commit + push**

```bash
git add tone_builder/target.py tone_builder/compare.py tone_builder/render.py tests/test_target.py tests/test_compare.py
git commit -m "target/compare: entries carry their frequencies; one reading for notes and chords"
git fetch && git pull --rebase && git push
```

---

### Task 7: alvo de acorde

**Files:**
- Modify: `tone_builder/target.py`
- Test: `tests/test_target.py`

**Interfaces:**
- Consumes: `tone_analyzer.chords.detect_chords`, `COLLIDE`; `read_levels`, `in_window`, `Window` (Tasks 5–6).
- Produces: `DEFAULT_DETECTOR = "salience"`; `chord_freqs(midis) -> list[float]`; `build_chord_target(disc, lead, window=None, detector=DEFAULT_DETECTOR, stats=None) -> list[dict]`, entradas `{"kind": "chord", "start_s", "midis", "name": "E2+B2+E3", "freqs_hz", "level_db", "accepted"}`; `stats` (dict) acumula `chords_seen`, `chords_few_harmonics`, `outside_window` e, em `build_target`, `notes_seen`, `notes_no_library`, `notes_few_harmonics`, `outside_window`.

- [ ] **Step 1: Testes que falham**

```python
from tone_builder.target import build_chord_target, chord_freqs


def _chord(midis, **k):
    return sum(note(m, **k) for m in midis)


def test_chord_frequencies_drop_shared_harmonics():
    f = chord_freqs([40, 52])          # E2 and E3: every E3 harmonic is an E2 harmonic
    e2 = midi_hz(40)
    assert all(abs(x / e2 - round(x / e2)) < 0.03 for x in f)
    assert not any(abs(x - 2 * e2) / x < 0.024 for x in f)


def test_chord_target_reads_the_record_on_each_note():
    x = _chord([40, 47, 52], n_harm=12)
    t = build_chord_target(x, x)
    assert len(t) == 1 and t[0]["kind"] == "chord" and t[0]["midis"] == [40, 47, 52]
    assert t[0]["name"] == "E2+B2+E3"
    assert sum(t[0]["accepted"]) >= MIN_HARMONICS


def test_chord_target_honours_the_window_and_counts():
    x = _chord([40, 47, 52], n_harm=12)
    stats = {}
    assert build_chord_target(x, x, window=(1.0, None), stats=stats) == []
    assert stats["outside_window"] == 1
```

- [ ] **Step 2: Rodar e ver falhar** — `.venv/bin/pytest tests/test_target.py -v` → FAIL (`ImportError: build_chord_target`)

- [ ] **Step 3: Implementar**

```python
from tone_analyzer.chords import COLLIDE, detect_chords

DEFAULT_DETECTOR = "salience"   # decided by `validate --chords` (Task 11)


def chord_freqs(midis: list[int]) -> list[float]:
    """k*f0 of every note; a frequency within COLLIDE of another note's frequency has no
    single owner, so it leaves the target."""
    all_f = [(m, f) for m in midis for f in note_freqs(m)]
    keep = [f for m, f in all_f
            if not any(m2 != m and abs(f - f2) / f < COLLIDE for m2, f2 in all_f)]
    return sorted(keep)


def _count(stats: dict | None, key: str) -> None:
    if stats is not None:
        stats[key] = stats.get(key, 0) + 1


def build_chord_target(disc: np.ndarray, lead: np.ndarray, window: Window | None = None,
                       detector: str = DEFAULT_DETECTOR, stats: dict | None = None) -> list[dict]:
    """Chords located on the separated track, read on the record at each note's own harmonics."""
    out = []
    span = int(DUR_S * SR)
    for c in detect_chords(lead, SR, dur_s=DUR_S, detector=detector):
        _count(stats, "chords_seen")
        if not in_window(c["start_s"], window):
            _count(stats, "outside_window")
            continue
        if int(round(c["start_s"] * SR)) + span >= min(len(lead), len(disc)):
            continue
        freqs = chord_freqs(c["midis"])
        r = read_levels(disc, c["start_s"], freqs)
        if r is None or sum(r[1]) < MIN_HARMONICS:
            _count(stats, "chords_few_harmonics")
            continue
        out.append({"kind": "chord", "start_s": c["start_s"], "midis": c["midis"],
                    "name": "+".join(midi_name(m) for m in c["midis"]),
                    "freqs_hz": freqs, "level_db": r[0], "accepted": r[1]})
    return out
```

Em `build_target`, os mesmos contadores: `_count(stats, "notes_seen")` por nota detectada, `outside_window`, `notes_no_library` (midi fora da biblioteca), `notes_few_harmonics`.

- [ ] **Step 4: Rodar** — `.venv/bin/pytest -q` → PASS

- [ ] **Step 5: Commit + push**

```bash
git add tone_builder/target.py tests/test_target.py
git commit -m "target: chords located on the separated track, read on the record at each note's harmonics"
git fetch && git pull --rebase && git push
```

---

### Task 8: DI do acorde — voicings, soma e acordes gravados

**Files:**
- Create: `tone_builder/chords.py`
- Modify: `tone_builder/library.py` (nome e leitura de acorde gravado)
- Test: `tests/test_chords.py`

**Interfaces:**
- Consumes: `library.parse_note_filename`, `library.STANDARD_TUNING`, `audio.load_mono`, `tone_analyzer.take.take_onset`.
- Produces:
  - `library.chord_filename(voicing: list[tuple[int, int]], take: int) -> str` → `"c6-40_c5-47_c4-52-t1.wav"`; `library.parse_chord_filename(name) -> list[tuple[int, int]] | None` (lista `(corda, midi)`); `library.list_chords(root, guitar, position) -> list[Path]` (em `<position>/acordes/`).
  - `chords.voicings(midis, by_midi) -> list[list[Path]]` (≤ 32, ordenados por abertura e depois casa mais baixa).
  - `chords.sum_di(paths, out, offsets_s=None) -> Path`.
  - `chords.recorded_by_midis(root, guitar, position) -> dict[tuple[int, ...], list[Path]]`.
  - `chords.di_candidates(midis, by_midi, recorded, workdir) -> list[tuple[Path, str]]`, fonte `"summed"` ou `"recorded"`.

- [ ] **Step 1: Testes que falham** (`tests/test_chords.py`)

```python
import numpy as np

from tests.synth import SR, note, write
from tone_builder import chords, library
from tone_builder.audio import load_mono


def _lib(tmp_path, pairs):
    """pairs: (string, midi) -> a library note file."""
    out = {}
    for s, m in pairs:
        p = write(tmp_path / "lib" / library.note_filename(s, m), note(m, start_s=0.02 + 0.01 * s))
        out.setdefault(m, []).append(p)
    return out


def test_voicing_uses_distinct_strings_and_a_4_fret_hand(tmp_path):
    by = _lib(tmp_path, [(6, 40), (5, 47), (6, 47), (4, 52), (5, 52)])
    vs = chords.voicings([40, 47, 52], by)
    strings = [[library.parse_note_filename(p.name)[0] for p in v] for v in vs]
    assert [6, 5, 4] in strings
    assert all(len(set(s)) == 3 for s in strings)
    assert [6, 6, 5] not in strings


def test_voicing_rejects_a_stretch_over_4_frets(tmp_path):
    by = _lib(tmp_path, [(6, 41), (5, 50)])     # fret 1 and fret 5
    assert chords.voicings([41, 50], by) == []


def test_summed_di_aligns_attacks(tmp_path):
    by = _lib(tmp_path, [(6, 40), (5, 47)])
    out = chords.sum_di([by[40][0], by[47][0]], tmp_path / "c.wav")
    x = load_mono(out)
    from tone_analyzer.take import take_onset
    assert abs(take_onset(x, SR) / SR - chords.PREROLL_S) < 0.01
    assert np.abs(x).max() <= 1.0


def test_recorded_chord_is_a_candidate(tmp_path):
    by = _lib(tmp_path, [(6, 40), (5, 47)])
    root = tmp_path / "root"
    write(root / "g" / "pos5" / "acordes" / library.chord_filename([(6, 40), (5, 47)], 1), note(40))
    rec = chords.recorded_by_midis(root, "g", "pos5")
    cands = chords.di_candidates([40, 47], by, rec, tmp_path / "work")
    assert {src for _, src in cands} == {"summed", "recorded"}


def test_chord_filename_round_trip():
    v = [(6, 40), (5, 47), (4, 52)]
    assert library.parse_chord_filename(library.chord_filename(v, 2)) == v
```

- [ ] **Step 2: Rodar e ver falhar** — `.venv/bin/pytest tests/test_chords.py -v` → FAIL (`ModuleNotFoundError: tone_builder.chords`)

- [ ] **Step 3: Implementar**

`library.py`:

```python
_CHORD_RE = re.compile(r"^((?:c[1-6]-\d+_)+c[1-6]-\d+)-t(\d+)\.wav$")
CHORDS_DIR = "acordes"


def chord_filename(voicing: list[tuple[int, int]], take: int) -> str:
    return "_".join(f"c{s}-{m}" for s, m in voicing) + f"-t{take}.wav"


def parse_voicing(text: str) -> list[tuple[int, int]]:
    return [(int(p[1]), int(p.split("-")[1])) for p in text.split("_")]


def parse_chord_filename(name: str) -> list[tuple[int, int]] | None:
    m = _CHORD_RE.match(name)
    return parse_voicing(m.group(1)) if m else None


def list_chords(root: Path, guitar: str, position: str) -> list[Path]:
    d = root / guitar / position / CHORDS_DIR
    return sorted(p for p in d.iterdir() if parse_chord_filename(p.name)) if d.is_dir() else []
```

(atualizar o docstring do módulo com `biblioteca/<guitar>/<position>/acordes/c6-40_c5-47-t1.wav`.)

`tone_builder/chords.py`:

```python
"""The DI of a chord: the library's single notes summed per playable voicing, or a chord
recorded in DI. Which one is closer is measured, like the string of a note."""

from __future__ import annotations

import itertools
from pathlib import Path

import numpy as np
import soundfile as sf
from tone_analyzer.take import take_onset

from tone_builder import library
from tone_builder.audio import SR, load_mono

MAX_SPAN = 3          # fretted notes within 4 frets (max - min <= 3); open strings do not count
MAX_VOICINGS = 32
PREROLL_S = 0.02


def _fret(string: int, midi: int) -> int:
    return midi - library.STANDARD_TUNING[string]


def voicings(midis: list[int], by_midi: dict[int, list[Path]]) -> list[list[Path]]:
    per = [[(library.parse_note_filename(p.name)[0], p) for p in by_midi.get(m, [])] for m in midis]
    found = []
    for combo in itertools.product(*per):
        strings = [s for s, _ in combo]
        if len(set(strings)) != len(strings):
            continue
        fretted = [f for f in (_fret(s, m) for (s, _), m in zip(combo, midis)) if f > 0]
        span = max(fretted) - min(fretted) if fretted else 0
        if span > MAX_SPAN:
            continue
        found.append((span, min(fretted, default=0), [p for _, p in combo]))
    found.sort(key=lambda t: (t[0], t[1], [str(p) for p in t[2]]))
    return [v for _, _, v in found[:MAX_VOICINGS]]


def sum_di(paths: list[Path], out: Path, offsets_s: list[float] | None = None) -> Path:
    """Each note's attack placed at PREROLL_S (+ its offset), summed, peak scaled to the loudest note."""
    offsets_s = offsets_s or [0.0] * len(paths)
    parts, peak = [], 0.0
    for p, off in zip(paths, offsets_s):
        x = load_mono(p)
        a = take_onset(x, SR) or 0
        lead = int(round((PREROLL_S + off) * SR))
        parts.append(np.concatenate([np.zeros(lead), x[a:]]))
        peak = max(peak, float(np.abs(x).max()))
    n = max(len(x) for x in parts)
    y = sum(np.pad(x, (0, n - len(x))) for x in parts)
    y = y * (peak / (np.abs(y).max() + 1e-12))
    out.parent.mkdir(parents=True, exist_ok=True)
    sf.write(str(out), y.astype(np.float32), SR, subtype="FLOAT")
    return out


def recorded_by_midis(root: Path, guitar: str, position: str) -> dict[tuple[int, ...], list[Path]]:
    out: dict[tuple[int, ...], list[Path]] = {}
    for p in library.list_chords(root, guitar, position):
        key = tuple(sorted(m for _, m in library.parse_chord_filename(p.name)))
        out.setdefault(key, []).append(p)
    return out


def di_candidates(midis: list[int], by_midi: dict[int, list[Path]],
                  recorded: dict[tuple[int, ...], list[Path]], workdir: Path) -> list[tuple[Path, str]]:
    cands = []
    for v in voicings(midis, by_midi):
        name = "_".join(p.stem.split("-")[0] + "-" + p.stem.split("-")[1] for p in v)
        cands.append((sum_di(v, Path(workdir) / f"{name}.wav"), "summed"))
    cands += [(p, "recorded") for p in recorded.get(tuple(sorted(midis)), [])]
    return cands
```

- [ ] **Step 4: Rodar** — `.venv/bin/pytest -q` → PASS

- [ ] **Step 5: Commit + push**

```bash
git add tone_builder/chords.py tone_builder/library.py tests/test_chords.py
git commit -m "chords: DI of a chord from summed library notes per playable voicing, or recorded"
git fetch && git pull --rebase && git push
```

---

### Task 9: build com acordes — escolha do DI, relatório e diagnóstico do exit 4

**Files:**
- Modify: `tone_builder/strings.py` (`choose_dis`; `choose_strings` vira atalho)
- Modify: `tone_builder/build.py` (`build_tone`)
- Modify: `tone_builder/cli.py` (`_build`: detector, raiz da biblioteca para acordes gravados)
- Modify: `tone_builder/report.py` só se `to_markdown` listar notas (mostrar `source`)
- Test: `tests/test_build.py`

**Interfaces:**
- Consumes: `build_chord_target`, `DEFAULT_DETECTOR` (Task 7), `di_candidates` (Task 8).
- Produces: `choose_dis(target, candidates_for, render, workdir) -> list[dict]`, cada `{"note", "di", "source", "deviation", "alternatives"}`; `build_tone(disc, lead, by_midi, research, device, workdir, name, window=None, chords=None)` com `chords = {"detector": str, "recorded": dict}` ou `None` (sem acordes); `report["notes"][i]` ganha `"kind"` e `"source"`; `ValueError` do alvo vazio traz as contagens de `stats`.

- [ ] **Step 1: Testes que falham** (`tests/test_build.py`)

O `FakeDevice.renderer` hoje tira o midi do nome do DI. Para acorde o DI somado se chama `c6-40_c5-47.wav`: fazer o renderer copiar o DI aplicando os ganhos por harmônico de cada nota. Acrescentar:

```python
from tone_builder import library as lib_mod


class ChordDevice(FakeDevice):
    """Renders any DI by re-synthesizing each note found in its file name with the chain's gains."""

    def renderer(self, blocks):
        total = np.sum([b["gains"] for b in blocks], axis=0) if blocks else np.zeros(8)

        def render(src, dst):
            if "_" in src.stem:      # summed chord DI: c6-40_c5-47_c4-52.wav
                midis = [int(t.split("-")[1]) for t in src.stem.split("_")]
            else:
                midis = [lib_mod.parse_note_filename(src.name)[1]]
            y = sum(note(m, start_s=0.02, gains_db=list(total)) for m in midis)
            sf.write(str(dst), y, 48000, subtype="FLOAT")
        return render


def test_build_on_a_chord_only_part(tmp_path):
    chord = [40, 47, 52]
    disc = np.concatenate([sum(note(m, gains_db=TARGET) for m in chord) for _ in range(6)])
    by_midi = {m: [write(tmp_path / "lib" / lib_mod.note_filename(s, m), note(m, start_s=0.02))]
               for s, m in zip((6, 5, 4), chord)}
    amp = _opt("amp1", "amp", "Amp", [0.0] * 8)
    drive = _opt("ts", "single_drive", "TS", TARGET)
    dev = ChordDevice({"amp": [amp], "single_drive": [drive]})
    res = build_tone(disc, disc, by_midi, RESEARCH, dev, tmp_path / "w", "x",
                     chords={"detector": "salience", "recorded": {}})
    kinds = {n["kind"] for n in res["report"]["notes"]}
    assert kinds == {"chord"}
    assert {n["source"] for n in res["report"]["notes"]} == {"summed"}
    assert res["report"]["final_deviation_db"] is not None


def test_empty_target_says_what_was_seen(tmp_path):
    chord = [40, 47, 52]
    disc = sum(note(m) for m in chord)
    dev = ChordDevice({"amp": [_opt("amp1", "amp", "Amp", [0.0] * 8)]})
    with pytest.raises(ValueError, match=r"chords_seen=1"):
        build_tone(disc, disc, {}, RESEARCH, dev, tmp_path / "w", "x",
                   chords={"detector": "salience", "recorded": {}})
```

- [ ] **Step 2: Rodar e ver falhar** — `.venv/bin/pytest tests/test_build.py -v` → FAIL (`unexpected keyword argument 'chords'`)

- [ ] **Step 3: Implementar**

`strings.py`:

```python
def choose_dis(target: list[dict], candidates_for, render: Renderer, workdir: Path) -> list[dict]:
    """candidates_for(entry) -> [(DI path, source)]; every candidate is rendered and measured."""
    out = []
    for i, entry in enumerate(target):
        alts: dict[str, float | None] = {}
        sources: dict[str, str] = {}
        for di, source in candidates_for(entry):
            x = render_note(render, di, workdir, f"di-{i:02d}-{Path(di).stem}")
            d = note_deviation(entry, x)
            alts[str(di)] = None if d is None else d["rms_db"]
            sources[str(di)] = source
        scored = [(v, k) for k, v in alts.items() if v is not None]
        if not scored:
            continue
        best_dev, best = min(scored)
        out.append({"note": entry, "di": Path(best), "source": sources[best], "deviation": best_dev,
                    "alternatives": alts})
    return out


def choose_strings(target: list[dict], by_midi: dict[int, list[Path]], render: Renderer,
                   workdir: Path) -> list[dict]:
    return choose_dis(target, lambda n: [(p, "library-note") for p in by_midi.get(n["midi"], [])], render, workdir)
```

(o tag antigo era `string-{i:02d}-…`; conferir se algum teste depende dele — `grep -rn "string-" tests` — e ajustar o teste, não o nome.)

`build.py`, trechos novos em `build_tone`:

```python
from tone_builder.chords import di_candidates
from tone_builder.strings import choose_dis
from tone_builder.target import build_chord_target, build_target

    stats: dict = {}
    target = build_target(disc, lead, set(by_midi), window=window, stats=stats)
    if chords is not None:
        target += build_chord_target(disc, lead, window=window, detector=chords["detector"], stats=stats)
        target.sort(key=lambda e: e["start_s"])
    if not target:
        seen = " ".join(f"{k}={v}" for k, v in sorted(stats.items()))
        raise ValueError(f"no target note or chord: nothing on the separated track matches the library "
                         f"with enough harmonics ({seen or 'no attack found'})")
    ...
    def candidates_for(entry):
        if entry["kind"] == "note":
            return [(p, "library-note") for p in by_midi.get(entry["midi"], [])]
        return di_candidates(entry["midis"], by_midi, chords["recorded"], workdir / "chords")

    assignments = choose_dis(target, candidates_for, device.renderer(assemble(state)), workdir / "strings")
```

e no relatório:

```python
    rep["notes"] = [{"name": a["note"]["name"], "kind": a["note"]["kind"], "start_s": a["note"]["start_s"],
                     "di": str(a["di"]), "source": a["source"], "deviation_db": d}
                    for a, d in zip(assignments, final["per_note"])]
```

Acorde sem nenhum voicing tocável e sem gravado sai de `choose_dis` (lista vazia de candidatos → `continue`); contar em `stats["chords_no_voicing"]` dentro de `candidates_for` quando a lista vier vazia.

`cli.py`, `_build`: `from tone_builder.chords import recorded_by_midis` e `from tone_builder.target import DEFAULT_DETECTOR`; argumento `--chord-detector` (default `DEFAULT_DETECTOR`, choices `("salience", "basic-pitch")`) e `--no-chords` (`store_true`); passar
`chords=None if a.no_chords else {"detector": a.chord_detector, "recorded": recorded_by_midis(_root(a), a.guitar, a.position)}`.
Mesmos dois argumentos em `target`, que passa a gravar notas + acordes e imprimir `n['name']` para os dois.

`report.py`: se `to_markdown` lista `notes`, acrescentar a coluna da origem (`library-note`/`summed`/`recorded`).

- [ ] **Step 4: Rodar tudo** — `.venv/bin/pytest -q` → PASS

- [ ] **Step 5: Commit + push**

```bash
git add tone_builder/strings.py tone_builder/build.py tone_builder/cli.py tone_builder/report.py tests/test_build.py
git commit -m "build: chords in the target, DI chosen by measurement (summed voicing or recorded); exit 4 says what was seen (issue #1)"
git fetch && git pull --rebase && git push
```

---

### Task 10: `library record-chord` e `check` de acordes

**Files:**
- Modify: `tone_builder/recorder.py` (`save_chord`)
- Modify: `tone_builder/cli.py` (`record-chord`, `_check`)
- Test: `tests/test_recorder.py`, `tests/test_cli.py`

**Interfaces:**
- Consumes: `recorder.record`, `library.chord_filename`, `library.parse_voicing`, `tone_analyzer.chords.detect_chords`, `tone_analyzer.notes.note_onsets`.
- Produces: `recorder.save_chord(root, guitar, position, voicing, signal, sr, takes=3) -> {"accepted": [take], "rejected": {take: reason}}`; CLI `library record-chord GUITAR POSITION VOICING --device D --channel N [--seconds 12]`.

- [ ] **Step 1: Testes que falham**

`tests/test_recorder.py`:

```python
def test_save_chord_keeps_takes_with_the_right_notes(tmp_path):
    from tests.synth import note as snote
    from tone_builder import library
    from tone_builder.recorder import save_chord
    chord = lambda ms: sum(snote(m, start_s=0.3) for m in ms)
    sig = np.concatenate([chord([40, 47, 52]), chord([40, 47, 52]), chord([41, 48, 53])])
    rep = save_chord(tmp_path, "g", "pos5", [(6, 40), (5, 47), (4, 52)], sig, 48000)
    assert rep["accepted"] == [1, 2] and 3 in rep["rejected"]
    assert len(library.list_chords(tmp_path, "g", "pos5")) == 2
```

`tests/test_cli.py`:

```python
def test_record_chord_requires_device_and_channel(tmp_path, capsys):
    assert cli.main(["library", "record-chord", "g", "pos5", "c6-40_c5-47", "--root", str(tmp_path)]) == 2
    assert "--device" in capsys.readouterr().err
```

- [ ] **Step 2: Rodar e ver falhar** — `.venv/bin/pytest tests/test_recorder.py tests/test_cli.py -v` → FAIL

- [ ] **Step 3: Implementar**

`recorder.py`:

```python
CHORD_TAKE_S = 2.0


def save_chord(root: Path, guitar: str, position: str, voicing: list[tuple[int, int]],
               signal: np.ndarray, sr: int, takes: int = 3) -> dict:
    """Up to `takes` strums; a take is kept only when the chord detector hears exactly its notes."""
    from tone_analyzer.chords import detect_chords
    from tone_analyzer.notes import note_onsets
    want = sorted(m for _, m in voicing)
    out_dir = root / guitar / position / library.CHORDS_DIR
    out_dir.mkdir(parents=True, exist_ok=True)
    rep: dict = {"accepted": [], "rejected": {}}
    for i, a in enumerate(note_onsets(signal, sr)[:takes], 1):
        start = max(0, a - int(PREROLL_S * sr))
        piece = signal[start:start + int(CHORD_TAKE_S * sr)]
        heard = detect_chords(piece, sr)
        got = heard[0]["midis"] if heard else []
        if got != want:
            rep["rejected"][i] = f"heard {got}, expected {want}"
            continue
        sf.write(out_dir / library.chord_filename(voicing, i), piece, sr, subtype="FLOAT")
        rep["accepted"].append(i)
    return rep
```

(Se `note_onsets` acha mais de um ataque por palhetada porque as cordas soam em sequência, usar o `min_sep_s=0.5` que ele já tem; o sintético do teste separa por 2,3 s.)

`cli.py`: `_record_chord(a)` igual a `_record` (mesma mensagem quando falta `--device`/`--channel`), chamando `recorder.save_chord(_root(a), a.guitar, a.position, library.parse_voicing(a.voicing), x, recorder.SR)`, imprimindo `accepted`/`rejected`, retorno 0 se `rejected` vazio. Parser `record-chord` com `guitar position voicing --device --channel --seconds (12.0) --root`; registrar em `{"list", "check", "record", "record-chord"}`.
`_check`: depois das notas, para cada `library.list_chords(...)` rodar `detect_chords` e imprimir `ok       acordes/<stem>` ou `REJECTED acordes/<stem>: heard …`, contando em `bad`.

- [ ] **Step 4: Rodar** — `.venv/bin/pytest -q` → PASS

- [ ] **Step 5: Commit + push**

```bash
git add tone_builder/recorder.py tone_builder/cli.py tests/test_recorder.py tests/test_cli.py
git commit -m "library: record-chord keeps strums whose notes the detector hears; check covers chords"
git fetch && git pull --rebase && git push
```

---

### Task 11: `validate --chords`

**Files:**
- Modify: `tone_builder/validator.py` (`known_truth_chords`, `SHAPES`)
- Modify: `tone_builder/cli.py` (`_validate`, flags `--chords`, `--detector`)
- Test: `tests/test_validator.py`

**Interfaces:**
- Consumes: `chords.voicings`, `chords.sum_di`, `target.chord_freqs`, `tone_analyzer.chords.detect_chords`, `levels_at`, `_pink`, `_bass`, `_unit_rms`.
- Produces: `known_truth_chords(by_midi, detector, dominance_db=-6.0, seed=0, workdir) -> {"chords", "set_ok_pct", "false_notes", "error_db", "false_positives", "dominance_db", "detector"}`; `summed_vs_recorded(by_midi, recorded, workdir) -> {"pairs", "error_db"}`; `CHORD_SET_OK_PCT = 90.0`.

- [ ] **Step 1: Teste que falha** (`tests/test_validator.py`)

```python
def test_known_truth_chords_on_synthetic_library(tmp_path):
    from tests.synth import note, write
    from tone_builder import library
    from tone_builder.validator import known_truth_chords
    by = {}
    for s, base in library.STANDARD_TUNING.items():
        for fret in range(0, 8):
            m = base + fret
            by.setdefault(m, []).append(write(tmp_path / "lib" / library.note_filename(s, m),
                                              note(m, start_s=0.02, n_harm=12)))
    r = known_truth_chords(by, "salience", dominance_db=6.0, workdir=tmp_path / "w")
    assert r["chords"] >= 4
    assert r["set_ok_pct"] >= 90.0 and r["false_notes"] == 0
```

- [ ] **Step 2: Rodar e ver falhar** — `.venv/bin/pytest tests/test_validator.py -v` → FAIL (`ImportError`)

- [ ] **Step 3: Implementar** (em `validator.py`)

```python
from tone_analyzer.chords import detect_chords, levels_at

from tone_builder import chords as chords_mod
from tone_builder.target import chord_freqs

CHORD_SET_OK_PCT = 90.0
STAGGER_S = 0.030      # a strum spreads the strings over up to 30 ms
# interval shapes over the root, by name
SHAPES = {
    "power2": [0, 7], "power3": [0, 7, 12],
    "major-E": [0, 7, 12, 16, 19, 24], "minor-E": [0, 7, 12, 15, 19, 24],
    "major-A": [0, 7, 12, 16, 19], "minor-A": [0, 7, 12, 15, 19],
}
ROOTS = range(40, 53)   # E2..E3


def known_truth_chords(by_midi: dict, detector: str, dominance_db: float = -6.0, seed: int = 0,
                       workdir: Path = Path("validate-chords")) -> dict:
    rng = np.random.default_rng(seed)
    span = int(DUR_S * SR)
    n = ok = false_notes = false_pos = 0
    errors = []
    for shape, iv in SHAPES.items():
        for root in ROOTS:
            midis = [root + i for i in iv]
            vs = chords_mod.voicings(midis, by_midi)
            if not vs:
                continue
            offs = sorted(rng.uniform(0, STAGGER_S, len(midis)))
            di = chords_mod.sum_di(vs[0], Path(workdir) / f"{shape}-{root}.wav", offsets_s=list(offs))
            x = load_mono(di)
            a = int(chords_mod.PREROLL_S * SR)
            guitar = x[a:a + span]
            if len(guitar) < span:
                continue
            guitar = 0.35 * guitar / (np.abs(guitar).max() + 1e-12)
            acc = _unit_rms(_unit_rms(_pink(span, rng)) + _unit_rms(_bass(span, rng)))
            g_rms = np.sqrt(np.mean(guitar ** 2))
            mix = np.concatenate([np.zeros(SR // 2), guitar + acc * g_rms / 10 ** (dominance_db / 20), np.zeros(SR // 2)])
            n += 1
            heard = detect_chords(mix, SR, dur_s=DUR_S, detector=detector)
            got = heard[0]["midis"] if heard else []
            ok += got == sorted(midis)
            false_notes += len(set(got) - set(midis))
            freqs = chord_freqs(sorted(midis))
            hm = levels_at(mix, SR, 0.5, freqs, dur_s=DUR_S)
            hg = levels_at(guitar, SR, 0.0, freqs, dur_s=DUR_S)
            if hm is None or hg is None:
                continue
            acc_k = [pm is not None and pm >= PROMINENCE_DB and lg is not None
                     for pm, lg in zip(hm["prominence_db"], hg["level_db"])]
            if sum(acc_k) < MIN_HARMONICS:
                continue
            false_pos += sum(1 for k, a_ in enumerate(acc_k)
                             if a_ and (hg["prominence_db"][k] is None or hg["prominence_db"][k] < NO_PEAK_DB))
            d = np.array([hm["level_db"][k] - hg["level_db"][k] for k, a_ in enumerate(acc_k) if a_])
            errors.append(float(np.sqrt(np.mean((d - d.mean()) ** 2))))
    return {"chords": n, "set_ok_pct": 100.0 * ok / n if n else None, "false_notes": false_notes,
            "error_db": float(np.mean(errors)) if errors else None, "false_positives": false_pos,
            "dominance_db": dominance_db, "detector": detector}


def summed_vs_recorded(by_midi: dict, recorded: dict, workdir: Path) -> dict:
    """Level error between a recorded chord and the library sum of the same voicing (informative)."""
    from tone_builder import library
    errors = []
    for paths in recorded.values():
        for p in paths:
            v = library.parse_chord_filename(p.name)
            notes = [next((q for q in by_midi.get(m, []) if library.parse_note_filename(q.name)[0] == s), None)
                     for s, m in v]
            if None in notes:
                continue
            summed = load_mono(chords_mod.sum_di(notes, Path(workdir) / f"sum-{p.stem}.wav"))
            rec = load_mono(p)
            freqs = chord_freqs(sorted(m for _, m in v))
            from tone_analyzer.take import take_onset
            hr = levels_at(rec, SR, (take_onset(rec, SR) or 0) / SR, freqs, dur_s=DUR_S)
            hs = levels_at(summed, SR, chords_mod.PREROLL_S, freqs, dur_s=DUR_S)
            if hr is None or hs is None:
                continue
            ks = [k for k, (a_, b_, pr) in enumerate(zip(hr["level_db"], hs["level_db"], hr["prominence_db"]))
                  if a_ is not None and b_ is not None and pr is not None and pr >= PROMINENCE_DB]
            if len(ks) < MIN_HARMONICS:
                continue
            d = np.array([hr["level_db"][k] - hs["level_db"][k] for k in ks])
            errors.append(float(np.sqrt(np.mean((d - d.mean()) ** 2))))
    return {"pairs": len(errors), "error_db": float(np.mean(errors)) if errors else None}
```

`cli.py`, `_validate`: com `--chords`, para cada `--detector` (default: os dois; o `basic-pitch` é pulado com aviso se o extra não estiver instalado) e cada dominância, imprimir
`detector  dominance  chords  set ok %  false notes  error dB  false positives`, e depois a linha de `summed_vs_recorded` com `recorded_by_midis(_root(a), a.guitar, a.position)`. Exit 1 quando o detector `DEFAULT_DETECTOR` a −6 dB tiver `set_ok_pct < CHORD_SET_OK_PCT`, `false_notes > 0` ou `error_db > MAX_ERROR_DB`. `workdir` = `tempfile.mkdtemp(prefix="tone-builder-validate-")`.

- [ ] **Step 4: Rodar** — `.venv/bin/pytest -q` → PASS

- [ ] **Step 5: Commit + push**

```bash
git add tone_builder/validator.py tone_builder/cli.py tests/test_validator.py
git commit -m "validate --chords: known-truth chords from the library, per detector, plus summed vs recorded"
git fetch && git pull --rebase && git push
```

---

### Task 12: medir na biblioteca real, escolher o detector, documentar e aceitar em *Even Flow*

**Files:**
- Modify: `tone_builder/target.py` (`DEFAULT_DETECTOR`, comentário com os números)
- Modify: `docs/metodo.md` (passo do acorde + tabela do `validate --chords`), `docs/learnings.md`
- Modify: `skills/tone-builder/SKILL.md` (**invocar `superpowers:writing-skills` antes**): `--from/--to`, `--role rhythm`, acordes, `library record-chord`
- Modify: `pyproject.toml`, `.claude-plugin/plugin.json` → `0.5.0`
- Test: `tests/test_real_library.py`

- [ ] **Step 1: Medir** (com LFS da biblioteca puxado: `git lfs pull --include "biblioteca/**"`)

```bash
.venv/bin/pip install -q "tone-analyzer[basic-pitch] @ git+https://github.com/jpfaria/tone-analyzer"
.venv/bin/tone-builder validate --chords --detector salience basic-pitch
```

Registrar a tabela inteira. Se nenhum detector passar o critério a −6 dB: **parar**, anotar os números no issue #1 só se o João pedir, e avisá-lo em uma linha com o pior número — não afrouxar o critério.

- [ ] **Step 2: Fixar o padrão** — `DEFAULT_DETECTOR` = o que passou com menor `error_db` (empate: maior `set_ok_pct`), comentário com data e números.

- [ ] **Step 3: Teste da biblioteca real** (`tests/test_real_library.py`)

```python
def test_chord_known_truth_on_the_shipped_library():
    notes = library.list_notes(ROOT, "prs-silver-sky-se", "pos5")
    if notes and _lfs_pointer(notes[0]):
        pytest.skip("LFS objects not pulled")
    from tone_builder.strings import library_by_midi
    from tone_builder.target import DEFAULT_DETECTOR
    from tone_builder.validator import CHORD_SET_OK_PCT, known_truth_chords
    r = known_truth_chords(library_by_midi(ROOT, "prs-silver-sky-se", "pos5"), DEFAULT_DETECTOR,
                           workdir=Path(__import__("tempfile").mkdtemp()))
    assert r["set_ok_pct"] >= CHORD_SET_OK_PCT and r["false_notes"] == 0 and r["error_db"] <= 2.0
```

Run: `.venv/bin/pytest -q` → PASS

- [ ] **Step 4: Docs e versão** — `metodo.md`: passo "1b. Acorde" com fonte do DI, colisão 2,4 %, voicing, e a tabela medida; `learnings.md`: o que a medição mostrou (inclusive se a soma erra contra o gravado, quando houver gravado); SKILL.md via `superpowers:writing-skills`; versões `0.5.0`.

- [ ] **Step 5: Aceite em *Even Flow*** (em background, a partir do clone; OpenRig precisa estar aberto — se o MCP/app não responder, rodar com `--device mvave`/`ampero2` conectado ou avisar o João em uma linha)

```bash
.venv/bin/tone-builder build --device openrig --artist "Pearl Jam" --song "Even Flow" --role rhythm \
  --research ~/.tone-builder/even-flow-pearl-jam/research.yaml --guitar prs-silver-sky-se --position pos5 \
  --name "DIG - Pearl Jam - Even Flow (base) tb-chords" --out ~/.tone-builder/even-flow-pearl-jam/openrig-chords-v1 \
  --plugins-root ~/Projetos/github.com/jpfaria/OpenRig-plugins/plugins/source --from 0:00 --to 2:50
```

Expected: `report.md` com notas `kind: chord` e `final_deviation_db` numérico. Relatar o status como escrito (`pronto`/`parcial`).

- [ ] **Step 6: Commit + push**

```bash
git add tone_builder/target.py docs/metodo.md docs/learnings.md skills/tone-builder/SKILL.md pyproject.toml .claude-plugin/plugin.json tests/test_real_library.py
git commit -m "chords: default detector measured on the real library; method, skill and 0.5.0 (closes #1)"
git fetch && git pull --rebase && git push
```
